/**
 * monitor/ — Extracted modules from the SessionMonitor god class.
 *
 * These modules handle specific concerns:
 * - dead-detector: Dead session detection and cleanup
 * - rate-limit-retry: Rate-limit signal handling and automatic retry
 * - status-broadcaster: Status change detection, debouncing, and broadcasting
 */
