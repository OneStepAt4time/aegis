# Template Gallery

Built-in starter templates consumed by `ag init --list-templates` and
`ag init --from-template <name>`.

Each template directory contains:

- `README.md` — human-readable summary of what the template scaffolds
- `scaffold/` — the files copied into the target project

The generated files are expected to stay self-documenting so `ag doctor` can
validate them after scaffolding.
