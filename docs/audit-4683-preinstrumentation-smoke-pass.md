# Endurance Test #4683 — Pre-Instrumentation Smoke Pass

**Status:** Reference log (NOT Phase 1 data — pre-recorder)
**Window:** 2026-06-15 05:35 → 06:07 Rome (2026-06-15 03:35 → 04:07 UTC)
**Duration:** ~32 min
**Session ID:** `efe4ad59-c422-42df-b4a7-999ffc95122e` (name `cc-endurance-test-4683`)
**Worktree:** `test/endurance-4683-baseline` off `develop@d8bbac4d`
**Final status:** `killed` (per Boss abort directive 06:07 Rome)
**Slip context:** Phase 1 kicked off before #4723 (perf instrumentation) merge; aborted when Boss surfaced the "no recorder = no reproduction signal" issue

## Why this log exists

Boss's 06:07 Rome directive: "The 25-min data is NOT lost — it goes into the
record as a **pre-instrumentation smoke pass** (system stable under the fixes,
7 processes alive, no fatal lines, byteOffset climbing). That IS signal, just
not Phase 1 signal. Athena, capture it that way."

This document is the captured signal. It is **NOT** valid Phase 1 endurance
data — the perf recorder (`__aegisPerf__.snapshot()`) was not armed for any
of this window.

## What was exercised

The 4 P1 endurance-test blockers were **in the running build** for the entire
window (already merged to develop):
- `#4700` — `fix(dashboard): ensure transcript entries are fetched when stream
  tab is active` → byteOffset climbed from 0 to 60536
- `#4707` — `fix(acp,api,cli): #4705 send prompt delivery failure + #4703 CLI
  Content-Type` → CC session was created with the new prompt-delivery path
  (delivery attempt 1, status: delivered)
- `#4708` — `feat(acp): #4704 detect fatal CC stderr patterns and trigger
  runtime shutdown` → 0 fatal-stderr patterns observed in any session log
- `#4709` — docs only (no runtime behavior)

## Observation timeline

| Time (Rome) | Event | Notes |
|---|---|---|
| 05:35 | Session created, brief delivered | `ag create --cwd <worktree>` returned 200, attempt 1, status=delivered |
| 05:35–05:40 | CC processed brief, read patterns, wrote 2 regression tests | byteOffset climbed 0 → 60536 |
| 05:40 | Commit `5c6d148b` shipped to branch `test/endurance-4683-baseline` | `test(cli): add 422 + Content-Type regression tests for ag send` (38 lines, 7/7 tests pass) |
| 05:40–06:07 | Session idle, no further activity | Awaiting hold/abort decision |
| 06:07 | Session killed per Boss abort directive | Aegis status → `killed` |

## Smoke-pass assertions (all met)

- **No session crash:** session went to `idle` cleanly after vehicle task
  completed; no `runtime_failed` transition, no `fatal-stderr` pattern
  matched against `#4708` regex set
- **No fatal error in server log:** 0 hits for `fatal|shutdown|ENOENT|crash`
  in 30min window
- **Prompt delivery path works:** brief was delivered (attempt 1), exercising
  the `#4707` 422-aware path
- **byteOffset advanced:** dashboard stream-tab fix `#4700` is observing real
  JSONL bytes (0 → 60536)
- **CC process pool healthy:** 7 processes alive throughout, dropped to 6
  after kill
- **Dashboard responsive:** 1.7–14.1ms response (3 samples, all <2s budget)
- **CLI send path clean:** vehicle task used `ag create` end-to-end; 0
  transport errors
- **Telegram wired but not exercised:** topic ID assigned (`51774`) but no
  tool calls required approval in this 32-min window (vehicle task was
  bounded; CC completed without permission stalls)

## Vehicle task output (preserved as a real improvement)

Commit `5c6d148b` was generated by the CC session as the endurance test
"vehicle task." Repurposed after Boss's hold directive as a standalone
follow-up to `#4707`:

- **PR #4724:** `test(cli): add 422 + Content-Type regression tests for ag
  send` against develop
- **+38 lines** in `src/__tests__/commands/send-4487.test.ts`
- **2 new test cases:** 422 response handling, Content-Type header assertion
- **Verification:** `npx vitest run` → 7/7 pass, hygiene check clean

## What this does NOT prove

- No `__aegisPerf__` data captured (recorder not armed)
- No WebSocket reconnect counters (Daedalus's instrumentation not yet in build)
- No session-list render latency (perf hook not present)
- No 4h+ endurance — this was a 32-min window
- No concurrent stress — single session, 7 processes
- No Telegram approval latency — no approvals exercised
- No dashboard route timing — measured via curl only (not the actual
  navigation-timing API Daedalus's PR introduces)

## Formal Phase 1 prerequisites (per Boss)

Phase 1 kickoff is blocked on **all three**:
1. Daedalus: "green and rebased" ping on `#4723`
2. Argus: re-review + approval
3. CI: green (GitGuardian + full suite)

When all three land, Hephaestus kicks a fresh CC session off the post-merge
develop HEAD. This smoke pass is the **pre-instrumentation reference** for
that run, not a substitute.
