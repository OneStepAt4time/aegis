# Daily Log — 2026-03-27 (Friday)

## Session Overview
Huge day — 19 PRs merged, 1037 tests passing (+137 from start). Aegis is now security-hardened and P0-complete.

## PRs Merged (19 total)

### Bug Fixes (12)
| PR | Issue | Description |
|-----|-------|-------------|
| #325 | — | Initial fix |
| #326 | #305 | Type guard `hasStatusCode()` for 404 detection |
| #327 | #306 | 4 race condition fixes (SessionTable, dedup, SSE flicker, orphan abort) |
| #328 | #257 | PermissionRequest hook mapped to `permission_prompt` |
| #329 | #258 | stateSince cleanup on non-idle transitions |
| #330 | #295 | Dashboard BASE_URL relative URL fix |
| #331 | #296 | Dead sessionMessages cleanup, MAX_MESSAGES=500 |
| #332 | #308 | Emitter cleanup race (WeakRef), idle SSE timeout, legacy route removal, Last-Event-ID, circuit-breaker, async emit |
| #333 | #302 | SSEWriter class — back-pressure, 3-strike disconnect |
| #334 | #298+#299 | isRetryableError(), generation counter debounce |
| #335 | #301 | Global event ring buffer with Last-Event-ID replay |
| #341 | #339 | Hook settings file merge — CC no longer loses env/permissions |

### Features (5)
| PR | Issue | Description |
|-----|-------|-------------|
| #337 | #310 | Live terminal via xterm.js + WebSocket (P0) |
| #338 | #312 | Batch session creation UI (P0) |
| #340 | #311 | Pipeline management page (P0) |
| #342 | #336 | Headless question answering — PreToolUse updatedInput (P1) |
| #343 | #300 | SSE connection limits — per-IP (10) + global (100) (Security) |

### Security (2)
| PR | Issue | Description |
|-----|-------|-------------|
| #344 | #303 | WS terminal auth + rate limiting + shared polls + ping/pong |
| #345 | #297 | Short-lived SSE tokens — avoids bearer token in URL query params |

## Key Lessons
- Finishing skill is NOT worktree-aware — must guide CC to push correct branch
- CC sometimes commits on main instead of worktree — need to verify and reset
- Local main can diverge from origin after squash merges — always `git reset --hard origin/main`
- Perplexity Search API quota can be exhausted — limit competitor research
- Workflow skill takes 15-30 min per task (design → implementation → verification → PR)

## Metrics
- Tests: 898 → 1037 (+137, +15.2%)
- Test files: 50 backend + 9 dashboard = 59
- Open bugs: 12 → 0
- Open security issues: 3 → 0
- P0 features: 3 → 0
- Aegis uptime: ~10h continuous

## Stale Sessions
All stale CC sessions cleaned up by end of day. No orphaned worktrees.

## Late Night Session (20:07 - 00:40)

Continued from afternoon session. Systematic bug + security sweep.

### PRs Merged (12 total this session: #367-#381)

| PR | Issue | Description |
|-----|-------|-------------|
| #367 | #353 | Swarm parent matching via PID |
| #368 | #356 | Monitor stall detection edge cases (7 fixes) |
| #369 | #361 | Graceful shutdown and crash recovery (9 fixes, 5 files) |
| #374 | #359 | Input validation across API routes (+452 lines, Zod schemas) |
| #375 | #362 | Terminal parser edge cases (5 fixes) |
| #376 | #358 | Shell injection in tmux.ts |
| #377 | #347 | Command injection in hook.ts via TMUX_PANE |
| #379 | #346 | SSRF protection for webhooks (+633 lines, new ssrf.ts) |
| #380 | #349 | Auth bypass via broad path matching |
| #381 | #348 | Authentication on inbound Telegram messages |

### Key Metrics
- Tests: 975 → 1176 (+201, +20.6%)
- All bugs closed: #353, #356, #359, #361, #362
- All security closed: #346, #347, #348, #349, #358
- Opened #378: flaky test channels.test.ts

### Lessons Learned
- Workflow skill frequently skips git commit — always verify with git log
- CC sometimes ignores worktree skill and commits on main — salvage immediately
- CC paste via send-keys sometimes needs an extra Enter to trigger processing
- Test flaky in channels.test.ts (jittered backoff) — just rerun CI, don't investigate unrelated code
