# 2026-03-22 — Day Zero

## Startup (03:00-03:30)
- Letto tutto il codebase Aegis (2,290 righe, 13 file)
- Letto feedback Zeus completo (cc-bridge-feedback.md)
- Tool MCP non disponibili nella sessione → workaround con `curl` + GitHub API
- Aegis API running su :9100, risponde, 0 sessioni attive

## Codebase Map
- `session.ts` (484L) — lifecycle, state, JSONL discovery, session_map sync
- `server.ts` (498L) — Fastify HTTP API, 14 endpoint (versioned + compat)
- `terminal-parser.ts` (268L) — 6 UI patterns, spinner detection, idle prompt
- `tmux.ts` (260L) — window create/kill, send-keys, capture-pane, JSONL archive
- `transcript.ts` (248L) — JSONL parser, byte offset reader, session file finder
- `monitor.ts` (230L) — 2s poll loop, stall detection 60min, idle debounce 10s
- `config.ts` (157L) — file + env config loader
- `hook.ts` (145L) — CC SessionStart hook, session_map.json writer
- `channels/` — types, manager, telegram (topics, bidirectional), webhook

## Zeus Feedback — Bug Status
### FIXED
- ✅ Session stall 4h no notification → stall watchdog (v1.1.0)
- ✅ Orphan idle notifications → cleanup on DELETE (v1.1.0)
- ✅ `--no-resume` non esiste → archive .jsonl approach (v1.2.0)
- ✅ Stale claudeSessionId from hook → cleanSessionMapForWindow (v1.2.1)
- ✅ Idle spam → one-shot idle notification (v1.2.1)

### STILL OPEN
- ⚠️ claudeSessionId intermittente (D30 got stale id, cleanSessionMap non sempre funziona)
- 🔴 CRITICAL: CC Bridge reuses OLD sessions (3 tentativi D51 falliti, impossibile lanciare task nuovi)
- 🔴 Session spawn failure (tmux windows non create, claude non parte, byteOffset 0 forever)

## Key Insight
Il P0 "prompt delivery" dalle issue GitHub è importante, ma il CRITICAL dal feedback Zeus è più urgente:
- Session reuse bug → Claude risponde con contesto di sessioni vecchie
- Session spawn failure → tmux windows non vengono create dopo uptime prolungato
- Entrambi sono nella stessa area: `session.ts` + `tmux.ts` + `session_map.json` sync

## Architecture Note (from Ema)
v2.0 sarà backend-agnostic. Pensare a interfacce: `SessionBackend`, `TranscriptParser`, `TerminalDetector`.
Non hardcodare behavior CC-specifico dove si può astrarre.

## Security Rule
MAI committare secrets. Verifica pre-commit con grep. Vedi TOOLS.md.

## Fixes Shipped (03:30-03:55)

### Issue #6 — CRITICAL session reuse (PR #8, merged)
- 3 guards: written_at timestamp, _archived/ path rejection, JSONL mtime check
- Key bug: `break` instead of `continue` when rejecting stale entries
- 11 tests added

### Issue #7 — Session spawn failure (PR #9, merged)
- ensureSession() now verifies health via list-windows
- createWindow() retries 3x with backoff
- Post-spawn verification: checks pane command to detect if Claude didn't start
- 9 tests added

### Issue #1 — Prompt delivery verification (PR #10, merged)
- verifyDelivery(): capture-pane, check working state or text match
- sendKeysVerified(): retry 3x with backoff (500/1500/3000ms)
- API returns { delivered, attempts } in POST /send response
- 16 tests added

### Test count: 136 → 208 (+72 tests, +53%)

### Issue #3 — Rename manus→aegis (PR #11, merged)
- Full backward compat: MANUS_* env vars, ~/.manus/ paths still work
- AEGIS_* takes priority over MANUS_*
- 24 config tests (was 15)

### Issue #2 — Session health check endpoint (PR #12, merged)
- GET /v1/sessions/:id/health
- alive = window exists AND (Claude running OR active in 5min)
- 11 tests added

### Issue #4 — Per-session stall detection (PR #13, merged)
- POST /sessions accepts stallThresholdMs
- Default reduced 60min → 5min
- 13 tests added

### Test count: 136 → 274 (+138 tests, +101%)

## Day Zero Totals
- **6 issues closed** out of 7 (#1, #2, #3, #4, #6, #7)
- **6 PRs merged** (#8, #9, #10, #11, #12, #13)
- **138 new tests** (136 → 274)
- **CI green on all PRs**
- Only #5 (npx DX) remains — requires npm publish setup

### Issue #5 — CLI entry point (PR #14, merged)
- npx aegis-bridge, --help, --port, dep checks
- package.json: name=aegis-bridge, version 1.1.0

### PR #19 — Filesystem discovery fallback (merged)
- Zeus field bug: hooks not firing post-migration (old hook path)
- Fix: parallel filesystem + hook discovery for all sessions

### Issue #15 — StopFailure hook support (PR #17, merged)
### Issue #16 — --bare flag detection (PR #18, merged)
### Issue #20 — DX permission_prompt clarity (created, open)

### Deep Dive (11:00)
- Cloned claude-cookbooks + prompt-eng-interactive-tutorial
- Analyzed CC plugin system (11 official plugins)
- Analyzed Claude Agent SDK tutorials (4 notebooks)
- Key insight: Aegis is complementary to Agent SDK, not competitive
- Written references/anthropic-repos-analysis.md

## Remaining Open Issues
- #5 — npx aegis (npm publish pending, needs Ema decision)
- #20 — DX permission_prompt clarity

## Tool Situation
- 23 tool OpenClaw nativi disponibili
- 0 MCP server (github-mcp-server, zread non presenti)
- GitHub API via curl (repo pubblico, funziona con token per write ops)
